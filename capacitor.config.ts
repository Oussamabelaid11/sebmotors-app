import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sebconnect-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
